{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Oracle Basket Currencies",
  "description": "Currency codes used in the oracle basket (scripts format)",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "array",
    "description": "Single-element tuple wrapping the currency code",
    "minItems": 1,
    "maxItems": 1,
    "items": {
      "type": "string",
      "description": "ISO 4217 currency code",
      "pattern": "^[A-Z]{2,4}$"
    }
  }
}
